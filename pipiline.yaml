trigger:
  branches:
    exclude:
      - '*'

pool:
  vmImage: 'ubuntu-latest'

steps:
- task: UsePythonVersion@0
  inputs:
    versionSpec: '3.x'
    addToPath: true

- script: |
    python -m pip install --upgrade pip
    pip install awscli
  displayName: 'Install AWS CLI'

- script: |
    export AWS_ACCESS_KEY_ID=$(AKIAXFYQZFAIWU5T22H6)
    export AWS_SECRET_ACCESS_KEY=$(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)
 
  displayName: 'Configure AWS CLI'

- script: |
    aws configure list
    env | grep AWS
    aws s3 cp $(Build.SourcesDirectory)/pythonfile.py s3://cpktnwt/
  displayName: 'Copy Python file to S3'
